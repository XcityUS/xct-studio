export type ShotDraft = {
    description: string;
    camera?: string;
    audio?: string;
    durationSeconds?: number;
};

export type ScriptFileExtraction = {
    filename: string;
    text: string;
};
